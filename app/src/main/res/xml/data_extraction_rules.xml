<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TMDB credentials live in noBackupFilesDir, which Android always excludes. -->
    </cloud-backup>
    <device-transfer>
        <!-- TMDB credentials live in noBackupFilesDir, which Android always excludes. -->
    </device-transfer>
</data-extraction-rules>
